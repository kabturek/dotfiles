function complementary_pairs ( $K,$A ) {
    $size = count($A);
    $sum = 0;
    for ($i = 0; $i <= $size; $i++){
        for ($j = $i; $j < $size; $j++){
            if($A[i] + $A[j] == $K){
                $sum++;
            } 
        }
    }
    return $sum;
}
